apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    uid: llmbench-prom
    jsonData:
      # Must match prometheus.yml global.scrape_interval, or rate() windows in
      # the dashboard silently under-resolve.
      timeInterval: 5s
